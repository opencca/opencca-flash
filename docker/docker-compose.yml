services:
  opencca-flash:  # XXX: Must match SERVICE name in Makefile
    build:
      context: ../
      dockerfile: docker/Dockerfile
      args:
        BUILD_DATE: ${BUILD_DATE}
        BUILD_REF: ${BUILD_REF}
        BUILD_VERSION: ${BUILD_VERSION}
    image: ghcr.io/opencca/opencca-flash:latest # XXX: Must match SERVICE_URL in Makefile
    container_name: opencca-flash
    hostname: opencca-flash
    privileged: true
    network_mode: "host"
    extra_hosts:
      - "opencca-flash:127.0.0.1"
    volumes:
      - ../../:/opencca # project root
      - /dev:/dev
      - ~/.ccache:/root/.ccache  # Mount ccache directory
      - .bash_history:/root/.bash_history  # Persist Bash history
      - .fish_history:/root/.local/share/fish/fish_history  # Persist Fish history
      - .cache/:/root/.cache

    environment:
      - CCACHE_DIR=/root/.ccache

    working_dir: /opencca/opencca-flash
    stdin_open: true
    tty: true    